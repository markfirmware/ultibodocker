version: '1.0'
steps:
  BuildingDockerImage:
    title: Building Docker Image
    type: build
    image_name: markfirmware/ultibo-workshop
    working_directory: .
    dockerfile: ultibo-workshop.dockerfile
